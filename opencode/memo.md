
## opencode + GPT-OSS 20B(LM Studio)
トークンが4096を超えてしまうためエラーとなり先に進まない。
プロバイダはシステムで指定されたものを仕様
opencode auth login

## opencode + Qwen3 (LM Studio)
トークンが4096を超えてしまうためエラーとなり先に進まない。
プロバイダはシステムで指定されたものを仕様
opencode auth login

## opencode + GPT-OSS 20B(ollama)
opencode.jsonを編集しローカルのollamaが使用できるように。
質問は理解してくれるが、コードは生成しない。

